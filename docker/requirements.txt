# =============================================================================
# Extra Python packages installed into Isaac Sim's Python environment.
# Install path: /isaac-sim/python.sh -m pip install -r requirements.txt
#
# NOTE: Do NOT add packages that conflict with Isaac Sim's pre-installed deps
#       (numpy, torch, omni.*, pxr, etc.).  Check with:
#         /isaac-sim/python.sh -m pip list
# =============================================================================

# --- Development / debugging ---
debugpy
ipdb
rich

# --- Code quality ---
ruff

# --- Testing ---
pytest>=7.0
pytest-xdist
