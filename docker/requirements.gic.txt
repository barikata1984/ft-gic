# =============================================================================
# GIC Python dependencies — installed AFTER torch==2.4.0+cu121
#
# Excluded from this file (installed by dedicated Dockerfile layers):
#   - torch / torchvision / torchaudio  (Stage 3, pytorch.org wheel index)
#   - torch-scatter                     (Stage 6, pyg.org wheel index)
#   - diff-gaussian-rasterization       (Stage 7, git source)
#   - simple-knn                        (Stage 7, git source)
#   - pytorch3d                         (Stage 7, git source)
#   - ninja                             (apt: ninja-build)
# =============================================================================

# GIC core deps (pinned to match environment.yml where possible)
dearpygui==1.10.1
einops==0.7.0
imageio==2.33.1
imageio-ffmpeg==0.4.9
lpips==0.1.4
mmcv==2.1.0
open3d==0.18.0
opencv-python==4.9.0.80
plyfile==1.0.3
scipy==1.12.0
taichi==1.7.4
tqdm==4.66.1
trimesh==4.1.0

# Dev tools (parity with Isaac Sim container)
debugpy
ipdb
rich
ruff
pytest>=7.0
pytest-xdist
